<!DOCTYPE html>
<html>
<head>
	<title>Project part 1 : Authentification</title>
	<link href="/style.css" rel="stylesheet" type="text/css" media="screen" />
	<meta http-equiv="content-type" content="text/html; charset=utf-8" />
</head>
<body>

	<h1><span class="right"><a href="/">Home</a></span>Authentification service</h1>

	<div id="content">
		<% if @error_session_message.nil? %>
			<p class="info">Thank to filling the form below to log in.</p>
		<% else %>
			<p class="error">Error while logging :<br />
			<% if @error_session_message == :session_not_exists %>
				The account with the username <strong><%= params['login'] %></strong> does not exist. <a href="/register/new">Click here to register you</a>.
			<% else %>
				The password does not match with the username <strong><%= params['login'] %></strong>
			<% end %>
			</p>
		<% end %>
	
		<div class="bloc">
			<h2>Log in</h2>
		
			<form method="post" action="<%= '/' + params['app_name'] unless params['app_name'].nil? %>/session/new">
				<div class="field">
					<label for="label_login">Username :</label>
					<input type="text" name="login" id="label_login" placeholder="Login" required />
				</div>
				<div class="field">
					<label for="label_password">Password :</label>
					<input type="password" name="password" id="label_password" placeholder="Password" required />
				</div>
			
				<input type="hidden" name="origin" value="<%= params['origin'] %>" />
			
				<div class="field_button">
					<input type="submit" value="Log in" />
				</div>
			</form>
		</div>
	</div>
	
</body>
</html>
